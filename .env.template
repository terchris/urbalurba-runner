# File: .env.template
#
# Purpose: Template for environment variables used to configure the urbalurba-runner
# Usage: Copy this file to .env and fill in the values before running the container
#        Command to copy: cp .env.template .env

# Required: Your GitHub Personal Access Token
GITHUB_PAT=your_github_personal_access_token_here

# Required: The GitHub organization to monitor
GITHUB_ORG=terchris

# Optional: Specific repository to monitor (set to urbalurba-runner for initial testing)
# Change this after verifying that the runner works with the urbalurba-runner repo
GITHUB_REPO=urbalurba-runner

# Optional: Custom name for the runner (leave blank to use hostname)
RUNNER_NAME=

# Optional: Custom labels for the runner (comma-separated)
RUNNER_LABELS=self-hosted,Linux,X64
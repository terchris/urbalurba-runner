# File: .env.template
#
# Purpose: Template for environment variables used to configure the urbalurba-runner
# Usage: Copy this file to .env and fill in the values before running the container
#        Command to copy: cp .env.template .env

# Required: Your GitHub Personal Access Token
# This token is used to authenticate the runner with your GitHub account
# Ensure it has the necessary permissions (repo and workflow scopes)
GITHUB_PAT=your_github_personal_access_token_here

# Required: Your GitHub username
# This is used to identify which account the runner is associated with
GITHUB_OWNER=your_github_username

# Required: The specific repository to monitor
# This defines which repository the runner will work with
# Example: urbalurba-runner
GITHUB_REPO=your_repository_name

# Optional: Custom name for the runner
# Leave blank to use the hostname as the runner name
# This helps identify the runner in GitHub Actions
RUNNER_NAME=

# Optional: Custom labels for the runner (comma-separated)
# These labels can be used in GitHub Actions workflows to target specific runners
# Default labels are set for a self-hosted Linux runner on ARM64 architecture
RUNNER_LABELS=self-hosted,Linux,ARM64